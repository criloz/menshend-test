menshend:
  image: nebtex/menshend:latest
  volumes:
    - ./config.yml:/etc/menshend.yml
  restart: always
  links:
    - vault
  env_file:
    - .menshend.env
  ports:
    - "8787:80"

vault:
  image: vault:latest
  restart: always
  ports:
    - "8200:8200"
  env_file: 
    - .vault.env